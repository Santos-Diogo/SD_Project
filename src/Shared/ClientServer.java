package Shared;

public class ClientServer 
{
    //defines the possible errors in execution
    public enum ExecError
    {

    }
    
    /**
     * returns an error message given the error
     * @param error
     * @return
     */
    public String errorMessage (ExecError error)
    {
        return null;
    }
}
